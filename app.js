const days=['อาทิตย์','จันทร์','อังคาร','พุธ','พฤหัส','ศุกร์','เสาร์'],sday=['อา','จ','อ','พ','พฤ','ศ','ส'];let data=JSON.parse(localStorage.getItem('narada632')||'null')||[{day:'จันทร์',time:'18:30',name:'ครูปาล์ม'},{day:'อังคาร',time:'16:30',name:'ไนไน'},{day:'อังคาร',time:'17:00',name:'ครูใบตอง'},{day:'พุธ',time:'18:15',name:'ครูปอย'},{day:'พฤหัส',time:'17:00',name:'ครูพลอย'},{day:'พฤหัส',time:'19:15',name:'ป้ามิ้น'}];const o={'จันทร์':1,'อังคาร':2,'พุธ':3,'พฤหัส':4,'ศุกร์':5,'เสาร์':6,'อาทิตย์':7};const now=new Date();const mon=new Date(now);mon.setDate(now.getDate()-((now.getDay()+6)%7));let active=days[now.getDay()],edit=-1;month.textContent=now.toLocaleDateString('th-TH',{month:'long',year:'numeric'});days.slice(1).concat(days[0]).forEach(x=>d.innerHTML+=`<option>${x}</option>`);function save(){localStorage.setItem('narada632',JSON.stringify(data))}function drawWeek(){week.innerHTML='';for(let i=0;i<7;i++){const dt=new Date(mon);dt.setDate(mon.getDate()+i);const full=days[dt.getDay()];const c=document.createElement('div');c.className='chip'+(full==active?' on':'');c.innerHTML=`${sday[dt.getDay()]}<br><b>${dt.getDate()}</b>`;c.addEventListener('click',()=>{active=full;drawWeek();drawList()});week.appendChild(c)}}function drawList(){list.innerHTML='';data.sort((a,b)=>o[a.day]-o[b.day]||a.time.localeCompare(b.time));const arr=data.filter(x=>x.day==active);if(!arr.length)list.innerHTML='<div class=card>ไม่มีคาบเรียน</div>';arr.forEach(it=>{const idx=data.indexOf(it);const el=document.createElement('div');el.className='card';el.innerHTML=`<div class=r><div><h2>${it.time}</h2><div>${it.name}</div></div><div><button class=e>✏️</button><button class=x>🗑️</button></div></div>`;el.querySelector('.e').addEventListener('click',()=>{edit=idx;d.value=it.day;t.value=it.time;n.value=it.name;modal.classList.remove('hide')});el.querySelector('.x').addEventListener('click',()=>{data.splice(idx,1);save();drawList()});list.appendChild(el)})}fab.addEventListener('click',()=>{edit=-1;d.value='จันทร์';t.value='16:00';n.value='';modal.classList.remove('hide')});c.onclick=()=>modal.classList.add('hide');s.onclick=()=>{const it={day:d.value,time:t.value,name:n.value||'ไม่มีชื่อ'};if(edit<0)data.push(it);else data[edit]=it;save();modal.classList.add('hide');drawWeek();drawList()};drawWeek();drawList();